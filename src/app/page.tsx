import { FormLogin } from "@/components/template/form-login";

export default async function Home() {
  return (
    <main className="main-h grid place-items-center mx-auto">
      <FormLogin />
    </main>
  );
}
